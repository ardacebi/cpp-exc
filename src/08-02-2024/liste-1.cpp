#include <iostream>
using namespace std;



int main() {
    struct esame {
        char nome[MAX];
        int cfu;
        int voto;
        esame* next;
    };

    esame* rifiuta_esame(esame* head, int cfu) {
    esame* now = head, * prev = NULL;

    }

    return 0;
}